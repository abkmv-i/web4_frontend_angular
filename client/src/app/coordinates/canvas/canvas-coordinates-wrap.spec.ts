import { CanvasCoordinatesWrap } from './canvas-coordinates-wrap';

describe('CanvasCoordinatesWrap', () => {
  it('should create an instance', () => {
    expect(new CanvasCoordinatesWrap()).toBeTruthy();
  });
});
